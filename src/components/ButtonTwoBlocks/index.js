//import { ButtonContainer } from "./styles";

const ButtonTwoBlocks = ({label, onClick, buttonTwoBlocks}) => {
    return (
        <ButtonTwoBlocks id={buttonTwoBlocks} onClick={onClick}>
            {label}
        </ButtonTwoBlocks>
    )
}

//export default Button;